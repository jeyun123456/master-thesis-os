# GitHub (private repo supported)
GITHUB_OWNER=jeyun123456
GITHUB_REPO=Obsidian-Vault
GITHUB_BRANCH=master
GITHUB_TOKEN=

# Optional path conventions inside the repo
GITHUB_RESULTS_PATH=projects/interim-presentation/코드/결과/주요결과/dashboard

# Server-only absolute path to the existing Obsidian-Vault checkout. Required for
# local Results fallback and exporter when this app is cloned as a separate repo.
# Do not set this in Vercel: production reads data through GitHub instead.
LOCAL_REPOSITORY_ROOT=

# Google Calendar (server-only OAuth; never prefix these with NEXT_PUBLIC_)
# Use a refresh token granted with calendar.readonly and offline access.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
# "primary" reads the authenticated user's primary calendar. A research calendar ID is also supported.
GOOGLE_CALENDAR_ID=primary

# Browser -> local bridge
NEXT_PUBLIC_LOCAL_BRIDGE_URL=http://127.0.0.1:38471
